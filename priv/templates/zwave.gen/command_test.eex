defmodule <%= inspect command_module %>Test do
  use ExUnit.Case, async: true

  alias <%= inspect command_module %>

  test "creates the command and validates params"

  test "encodes params correctly"

  test "decodes params correctly"
end
